## 🚀 QualityOS 统一触发器 v4.0

**触发入口**: `skills/quality-os-trigger/SKILL.md`

**自动触发规则**：
| 场景 | 触发时机 | 自动触发技能 |
|------|---------|-------------|
| 文档保存 | 文件保存时 | quality-prevention(事后 3Q) |
| 代码提交 | git commit 时 | quality-prevention(CODE) |
| 决策开始 | 决策需求创建 | decision-checklist |
| 子代理创建 | 任务下达时 | subagent-brief-template |
| 子代理交付 | 任务完成时 | 3Q 自动验收 |
| 内容发布 | 发布前 | 3Q-Plus-v3（强制检查） |

**任务分类决策树**：
```
任务输入 → 是否多模块整合？
  ├─ 是 → 整合类 (3Q-Max, S 级)
  └─ 否 → 是否方案选择？
      ├─ 是 → 决策类 (3Q-Decision, A 级)
      └─ 否 → 是否重复性任务？
          ├─ 是 → 机械类 (3Q-Lite, B 级)
          └─ 否 → 创作类 (3Q-Pro, A 级)
```

**质量指标**：
- 自动触发率：≥90%
- 平均评分：≥14/15
- S 级比例：≥50%
- 返工率：≤10%

---

## 🧠 技能触发检查清单（v4.0 自动化）

**自动触发流程**：
```
任务输入 → QualityOS 触发器 → 自动分类 → 匹配 3Q 版本 → 
生成任务简报 → 执行 → 质量检查 → 评分 → 知识库沉淀
```

### 文档类任务（自动触发）
- ✅ **task-breakdown-v3**：自动分类为创作类
- ✅ **quality-prevention-milestone-v2**：13 检查点 +3Q 整合
- ✅ **self-challenge-3q-v2**：事后 3Q 评分（15 分制）
- ✅ **3Q-Plus-v3**：重要文档发布前检查

### 代码类任务（自动触发）
- ✅ **task-breakdown-v3**：自动分类为创作类（CODE）
- ✅ **quality-prevention-milestone-v2(CODE)**：13 检查点（代码版）
- ✅ **self-challenge-3q-v2(CODE)**：3Q-CODE 评分
- ✅ **3Q-Plus-v3**：代码提交前元认知检查

### 决策类任务（自动触发）
- ✅ **decision-checklist-v2**：12 决策点 + 全流程 3Q
- ✅ **self-challenge-3q-v2(DECISION)**：3Q-Decision 评分
- ✅ **3Q-Plus-v3**：重大决策前元认知 Q+ 挑战 Q

### 子代理任务（自动触发）
- ✅ **subagent-brief-template-v3**：自动填写 3Q 要求
- ✅ **自动验收**：验证 3Q 评分 + 质量等级
- ✅ **3Q-Plus-v3**：子代理交付后自动验收

### 整合类任务（自动触发）
- ✅ **task-breakdown-v3**：自动分类为整合类
- ✅ **quality-prevention-milestone-v2**：13 检查点 +3Q-Max
- ✅ **self-challenge-3q-v2**：3Q-Max 评分（S 级要求）
- ✅ **3Q-Plus-v3**：系统整合完成后的全面验收

### 内容发布类任务（新增·强制触发）
- ✅ **3Q-Plus-v3**：内容发布前强制检查（小红书/公众号/知乎）
- ✅ **xiaohongshu-ops**：发布执行 → 自动触发 3Q-Plus 验收
- ✅ **quality-os-trigger**：发布前质量左移检查

**3Q-Plus 自动触发场景**：
| 场景 | 触发时机 | 检查重点 |
|------|---------|---------|
| 文档/内容发布前 | 发布动作执行前 | 元认知 Q + 挑战 Q + 验收 Q |
| 重要决策前 | 决策确认前 | 元认知 Q（目标对齐）+ 挑战 Q（风险识别） |
| 子代理交付后 | 任务完成时 | 验收 Q（交付质量验证） |
| 系统整合完成 | 模块交付后 | 全面 3Q 检查（逻辑/用户/竞争） |

**执行规则**：
1. **自动触发**：QualityOS 触发器自动识别任务类型
2. **质量左移**：事前 3Q 检查（5 分钟）
3. **事中检查**：13 检查点/12 决策点
4. **事后评分**：3Q 评分（15 分制）+ 质量等级
5. **知识沉淀**：3Q 答案模板入库

---

## 🎯 子代理空闲检查（✅ 已启用）

**状态**: ✅ **已启用**

**触发条件**: 子代理有空闲卡槽时自动填充新任务

**执行逻辑**:
1. 检查子代理并发状态（`subagents action=list`）
2. 如果有空闲槽位 → 读取 `ACTIVE-TASK.md` 获取待办任务
3. 按优先级启动新任务，填满卡槽
4. 卡槽数：4 个并发

**任务来源**: `ACTIVE-TASK.md` 中的"进行中"和"今日待办"部分

---

## 📊 质量指标追踪

**质量指标文件**: `quality-metrics.json`

```json
{
  "autoTriggerRate": 0.90,
  "avgScore": 14.0,
  "sGradeRatio": 0.50,
  "reworkRate": 0.10,
  "lastUpdate": "2026-03-30",
  "taskDistribution": {
    "mechanical": 0.40,
    "creative": 0.40,
    "decision": 0.10,
    "integration": 0.10
  }
}
```

**每周回顾**（心跳检查时）：
- 自动触发率是否≥90%？
- 平均评分是否≥14/15？
- S 级比例是否≥50%？
- 返工率是否≤10%？

**未达标 → 分析原因 → 改进**
